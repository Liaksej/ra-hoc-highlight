import { ArticleListItem } from "@/app/page";

export function Article(props: ArticleListItem) {
  return (
    <div className="item item-article">
      <h3>
        <a href="#">{props.title}</a>
      </h3>
      <p className="views">Прочтений: {props.views}</p>
    </div>
  );
}
